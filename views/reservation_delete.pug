extends layout

block content

    h1= title

    p Вы действительно хотите удалить эту бронь?

    form(method='POST' action='')
        div.form-group
            input#id.form-control(type='hidden',name='id', required='true', value=reservation._id )

        button.btn.btn-primary(type='submit') Удалить