extends layout

block content
    h1= title

    ul
        each waiter in waiters_list
            li
                a(href=waiter.url) #{waiter.name}
        else
            li There are no book copies in this library.