extends layout

block content
    h1= title

    ul
        each visitor in visitors_list
            li
                a(href=visitor.url) #{visitor.name}
        else
            li Нет посетителей.